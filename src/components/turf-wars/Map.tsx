
'use client';

// This is a placeholder for the map component.
// We will replace this with a real Leaflet map in the next step.
export function Map() {
    return (
        <div className="w-full h-full bg-muted/50 rounded-lg flex items-center justify-center border-2 border-dashed border-muted-foreground">
            <p className="text-muted-foreground">Map will be rendered here.</p>
        </div>
    )
}
